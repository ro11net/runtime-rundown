apiVersion: v1
kind: Service
metadata:
  name: {{ include "runtime-rundown.fullname" . }}
  labels:
    {{- include "runtime-rundown.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runtime-rundown.selectorLabels" . | nindent 4 }}
    app: runtime-rundown
