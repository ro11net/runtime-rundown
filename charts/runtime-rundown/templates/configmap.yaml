apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "flux-training-app.fullname" . }}-config
  labels:
    {{- include "flux-training-app.labels" . | nindent 4 }}
data:
  SECURITY_LEVEL: {{ .Values.app.env.SECURITY_LEVEL | quote }}
  MESSAGE: {{ .Values.app.env.MESSAGE | quote }}
