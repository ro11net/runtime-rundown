apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "runtime-rundown.fullname" . }}-config
  labels:
    {{- include "runtime-rundown.labels" . | nindent 4 }}
data:
  SECURITY_LEVEL: {{ .Values.app.env.SECURITY_LEVEL | quote }}
  MESSAGE: {{ .Values.app.env.MESSAGE | quote }}
